# Test artifacts (from our testing)
/.env
/.env
.env.age
.env.test
test.env
docker-compose.yaml
secure-compose

# Keep .env.age since it's the encrypted version
# But remove any local test .env files
*.log
.DS_Store
